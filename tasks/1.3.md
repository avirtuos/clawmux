Story: 1. Project Skeleton & Core Types
Task: 1.3
Status: COMPLETED

## Description

Implement all data model structs in src/tasks/models.rs: TaskId (with from_path and Display), TaskStatus (Display + FromStr, case-insensitive with underscore stripping), Task (with is_active()), Story (with sorted_tasks() numeric ordering), Question, WorkLogEntry.

Tests: test_task_status_display, test_task_status_from_str, test_task_id_from_path, test_story_sorted_tasks, test_story_sorted_tasks_double_digit, test_task_is_active.
