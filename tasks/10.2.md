Story: 10. Integration & Polish
Task: 10.2
Status: OPEN

## Description

Audit all error paths and eliminate unwrap()/expect() outside tests. Implement server restart logic in EventStreamConsumer (3 retries before emitting SessionError). Handle structured output parse failures in SessionCompleted without panicking. Add error_message: Option<String> to App and render a red-background error banner in the footer (Esc to clear). Implement Commands::Doctor in main.rs to check opencode binary and server health without opening TUI.

Tests: test_structured_output_parse_failure_does_not_panic, test_error_message_set_on_session_error, test_doctor_command_no_opencode.
