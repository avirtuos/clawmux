Story: 8. App Integration
Task: 8.2
Status: OPEN

## Description

Implement watch_task_files() in src/tasks/mod.rs using notify::recommended_watcher with NonRecursive mode. On Modify events for .md files, compute TaskId from path and send AppMessage::TaskFileChanged on the mpsc channel. Store the returned RecommendedWatcher in main.rs to keep it alive for the program's lifetime.

Tests: test_file_watcher_detects_modify, test_file_watcher_ignores_non_md.
