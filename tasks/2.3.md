Story: 2. Task File Parsing & Writing
Task: 2.3
Status: COMPLETED

## Description

Implement TaskStore in src/tasks/mod.rs with in-memory HashMap cache. Methods: new(), load_from_disk() (scans tasks/ then docs/tasks/, skips unparseable files with tracing::warn!), stories() (grouped and sorted numerically), get(), get_mut(), insert(), persist(), reload(), task_count().

Tests: test_load_from_disk_tasks_dir, test_load_from_disk_docs_tasks_fallback, test_stories_grouping, test_persist_roundtrip, test_reload_reflects_disk_change, test_get_missing_returns_none, test_task_count, test_skips_unparseable_files.
