Story: 3. Configuration System
Task: 3.2
Status: COMPLETED

## Description

Implement run_init() in src/config/init.rs with four steps: (1) check for opencode binary via which::which, offer to install; (2) load global config, prompt for provider credentials with rpassword if missing; (3) scaffold project files (.clawmux/config.toml, tasks/, .opencode/agents/clawmux/ with one .md per AgentKind); (4) print success summary. Wire Commands::Init in main.rs.

Tests: test_scaffold_creates_config, test_scaffold_creates_tasks_dir, test_scaffold_creates_all_agent_files, test_scaffold_idempotent, test_reset_agents_overwrites.
