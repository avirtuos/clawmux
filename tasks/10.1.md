Story: 10. Integration & Polish
Task: 10.1
Status: OPEN

## Description

Implement the full startup sequence in main.rs: initialize tracing_appender, parse CLI, load AppConfig, spawn OpenCodeServer, create OpenCodeClient, load TaskStore, create session_map, spawn EventStreamConsumer, spawn file watcher, initialize TUI, run event loop at ~16ms tick rate, restore terminal and call server.shutdown() on exit. Write integration tests using mockito to simulate the full workflow from StartTask through all 7 agents to PendingReview.

Tests: test_full_workflow_intake_to_completed, test_task_file_updated_after_agent_completion.
