Story: 5. OpenCode Client
Task: 5.4
Status: COMPLETED

## Description

Implement OpenCodeServer in src/opencode/server.rs. ensure_running() checks health; if ok returns with child: None (external mode). In auto mode, spawns opencode serve with injected env vars, polls health at exponential backoff (100ms..2000ms) for up to 30s. base_url() returns formatted URL. shutdown() sends SIGTERM and waits up to 5s. Wire into main.rs before TUI loop.

Tests: test_base_url_format, test_external_mode_health_fail, test_external_mode_health_ok.
