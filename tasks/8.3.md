Story: 8. App Integration
Task: 8.3
Status: COMPLETED

## Description

Wire together the 7-agent pipeline end-to-end:
1. Add `context` field to `CreateSession` and `response_text` field to `SessionCompleted` in `AppMessage`.
2. Remove `placeholder_prompt()` from transitions.rs; pass real context at each CreateSession site.
3. Create `src/workflow/response_parser.rs` with `AgentResponse` serde types and `parse_response()`.
4. Accumulate assistant text in `EventStreamConsumer` and include it in `SessionCompleted`.
5. Rewire `App::handle_message`: `StartTask` sets InProgress, `CreateSession` builds real prompt, `SessionCompleted` parses response and dispatches semantic message.
6. TUI: `Enter` on an OPEN task emits `StartTask`; footer hints updated.

